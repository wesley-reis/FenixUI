import { defineFxSelect } from './select';

// Reexporta a classe para uso programático (instanceof, herança).
export { FxSelect } from './select';

// Auto-registro ao importar o subpath `@fenix-ui/fenix-ui/select`.
defineFxSelect();
