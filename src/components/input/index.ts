import { defineFxInput } from './input';

// Reexporta a classe para uso programático (instanceof, herança).
export { FxInput } from './input';

// Auto-registro ao importar o subpath `@fenix-ui/fenix-ui/input`.
defineFxInput();
