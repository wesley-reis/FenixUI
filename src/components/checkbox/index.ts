import { defineFxCheckbox } from './checkbox';

// Reexporta a classe para uso programático (instanceof, herança).
export { FxCheckbox } from './checkbox';

// Auto-registro ao importar o subpath `@wrrdev/fenix-ui/checkbox`.
defineFxCheckbox();
