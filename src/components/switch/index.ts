import { defineFxSwitch } from './switch';

// Reexporta a classe para uso programático (instanceof, herança).
export { FxSwitch } from './switch';

// Auto-registro ao importar o subpath `@fenix-ui/fenix-ui/switch`.
defineFxSwitch();
